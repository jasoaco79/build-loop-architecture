# Build Loop Architecture

Iterate Consulting Chicago build-loop architecture microsite.

Plates locked 2026-09-19. **HOLD live deploy.** No DNS. No Duck cutover. Jason yes required before merge/deploy.

## Preview

```bash
python3 -m http.server 4173
```

Open `http://127.0.0.1:4173/`. See `HANDOFF.md` for serve notes and the `design/sot` plate source of truth.
