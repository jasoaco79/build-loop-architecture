# Chicago Build Loop

Iterate Consulting architecture microsite for the Chicago build loop.

**Four writers race. Codex reviews. Erlich picks the first clean PR. Jason says yes.**

Plates locked 2026-09-19 (01 hero … 07 lifecycle). Lockup B. Void `#0A0A0B`. Cyan `#22D3EE`. Playfair + Inter.

**HOLD live deploy.** No Duck cutover. Design only — no live execution.

```bash
python3 -m http.server 4173
```

See `HANDOFF.md`.
